const menuBtn=document.getElementById('menuBtn');const mobileMenu=document.getElementById('mobileMenu');const profileImage=document.getElementById('profileImage');const photoWrap=document.querySelector('.photo-wrap');menuBtn?.addEventListener('click',()=>mobileMenu.classList.toggle('open'));mobileMenu?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>mobileMenu.classList.remove('open')));profileImage?.addEventListener('load',()=>{if(profileImage.naturalWidth>0){photoWrap.classList.add('has-image')}});profileImage?.addEventListener('error',()=>photoWrap.classList.remove('has-image'));const reveals=document.querySelectorAll('.reveal');const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}})},{threshold:.16});reveals.forEach(item=>observer.observe(item));
